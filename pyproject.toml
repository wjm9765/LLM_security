[project]
name = "llm-safety-benchmark"
version = "0.1.0"
description = "Minimal LLM safety benchmark pipeline"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0.0",
    "transformers>=4.35.0",
    "accelerate>=0.21.0"
]
